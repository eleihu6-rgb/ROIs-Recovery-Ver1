/**
 * Toast — thin re-export wrapper around sonner.
 *
 * Usage in consuming apps:
 *   import { Toaster, toast } from "@rois/ui";
 *
 *   // Mount <Toaster /> once at app root.
 *   // Then call toast("Hello") / toast.success("Saved") anywhere.
 */
export { Toaster, toast } from "sonner";
