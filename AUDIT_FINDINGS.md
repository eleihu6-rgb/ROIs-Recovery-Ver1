# enhance-Ver1

| id | category | file + line range | problem | evidence | proposed_change | impact | effort | risk | depends_on |
|---|---|---|---|---|---|---|---|---|---|
| F-001 | performance | `live-server/src/routes/pbs/crew-bids.ts:78-212`; `gantt/src/components/scenario/crew-bids/crew-bids-view.tsx:379-403`; `gantt/src/components/scenario/crew-bids/crew-bids-view.tsx:280-337` | Crew Bids loads, transforms, and renders the entire matching bid dataset in one request/render cycle. | The route accepts only `periodCode`, `bidContext`, `base`, `rank`, and `crewId` with no page/limit fields, executes one unbounded aggregate query, returns all rows, the client stores `data.rows`, computes `filteredCrew.flatMap(flattenRows)`, and renders every row with `rows.map`. | Add equivalent pagination to `/api/pbs/crew-bids` using `page` and bounded `pageSize`, return `items/total/page/pageSize`, and update `CrewBidsView` to request pages or use row virtualization for the flattened bid table. | high — PBS bid periods can contain all crew and all bid groups; this is the clearest high-cardinality UI/API path and can degrade both database latency and browser rendering; exact latency needs measurement. | M | Pagination can change visible ordering or counts if the grouping boundary is wrong; verify with API tests for stable seniority/bid type order, total counts, and Playwright checks for first page, next page, search, base/rank filters, and empty states. |  |
| F-002 | performance | `live-server/src/routes/pbs/crew-bids.ts:118-180`; `sql/schema/03-pbs_pg.sql:130-132`; `sql/schema/03-pbs_pg.sql:512-516`; `sql/schema/03-pbs_pg.sql:225-229` | The Crew Bids query filters and joins on a wider column set than the current indexes cover. | The query filters by `b.period_code`, optionally `b.bid_context`, `pu.base`, `pu.rank`, joins `pu.crew_id = b.crew_id`, `g.bid_id = b.id`, and orders by `c.seniority_num`, while schema indexes are separate `idx_pbs_bid_period(period_code,bid_context)`, `uq_pbs_user_crew(crew_id)`, single-column `idx_pbs_user_base`, `idx_pbs_user_rank`, and `idx_pbs_bid_group_bid`. | Measure with `EXPLAIN (ANALYZE, BUFFERS)` on realistic PBS data, then add narrowly targeted composite indexes such as `(period_code, bid_context, crew_id)` on `pbs_bid` and `(base, rank, crew_id)` or partial variants on `pbs_user` only if the plan shows scans or poor join selectivity. | medium — better indexes can cut the highest-cardinality query path, but it is needs measurement because existing single-column indexes may be enough on small schemas. | S | Extra indexes slow PBS writes and migrations; verify with before/after query plans, route latency under representative data, and insert/update smoke tests for bid saves. | F-001 |
| F-003 | performance | `gantt/src/components/scenario/crew-bids/crew-bids-view.tsx:396-403`; `gantt/src/components/scenario/crew-bids/crew-bids-view.tsx:411-417`; `gantt/src/components/scenario/crew-bids/crew-bids-view.tsx:516-517` | Crew Bids recomputes filtered and flattened rows on every render even when the data and search text have not changed. | `filteredCrew` lowercases `searchText` inside each row predicate, `flatRows = filteredCrew.flatMap(flattenRows)` runs at component top level, and `flatRows` is then used for the header count and table props. | Wrap the derived data in `useMemo`, precompute `const normalizedSearch = searchText.trim().toLowerCase()`, and memoize `BidTable` or pass stable row arrays after F-001 pagination/virtualization. | medium — this removes repeated O(crew * bidGroups) work during typing, loading state changes, and refreshes; impact is lower if F-001 constrains page size. | S | Memoization can leave stale rows if dependencies are incomplete; verify search text, filter changes, refresh, and row counts update correctly. | F-001 |
| F-004 | performance | `live-server/src/services/scenario/scenario-export-service.ts:92-112` | RO scenario export materializes all table CSV sections and gzips them synchronously in the request path. | Each table result is converted to a full string with `rows.map(...).join('\n')`, all section strings are collected with `Promise.all`, then `gzipSync(Buffer.from(sections.join('\n'), 'utf-8'))` runs on the event loop. | Replace sync gzip and full-string assembly with a streaming or chunked export path using async zlib/pipeline, or move the compression work to a worker/job while preserving the same `ro_input.gz` bytes format. | medium — large scenarios can block the Fastify event loop and spike memory; exact data-volume thresholds need measurement. | M | Streaming can alter section ordering or CSV escaping if implemented carelessly; verify byte-level section order, CSV escaping tests, and an end-to-end RO export/import run. |  |
| F-005 | performance | `engine-server/main.py:35-47`; `engine-server/src/files/file_manager.py:89-177`; `engine-server/src/files/file_manager.py:179-215` | The async cleanup task runs blocking filesystem traversal, tar/gzip, and deletes directly on the event loop. | `_periodic_cleanup` awaits sleep and then calls synchronous `task_manager.cleanup_tasks()`, `file_manager.archive_files()`, and `file_manager.cleanup_expired_files()`; the file manager uses `os.listdir`, `os.walk`, `tarfile.open`, `gzip.open`, `shutil.copyfileobj`, `shutil.rmtree`, and `os.remove`. | Run cleanup with `asyncio.to_thread(...)` or an executor, keep one cleanup job at a time, and log duration/count metrics for archive and delete work. | medium — cleanup can stall health checks and API requests when archived engine outputs are large; exact stall duration needs measurement. | S | Threaded cleanup can race with task file moves if locks are not preserved; verify archive/cleanup tests, concurrent task completion, and graceful shutdown cancellation. |  |
| F-006 | performance | `data-migration/f8/crew.py:328-533`; `data-migration/f8/crew.py:558-575`; `data-migration/f8/pairing.py:390-588`; `data-migration/f8/roster_flight.py:188-261` | F8 import paths still perform row-at-a-time child inserts inside large batch loops. | Crew sync deletes and inserts child rows per crew; pairing sync inserts compositions, duties, nodes, segments, and synthesized flights per record; roster flight sync inserts each roster and roster_flight row individually despite preloading lookups. | Build per-batch row arrays and use `executemany` or multi-row inserts per child table while retaining per-record savepoints only where isolation is required, starting with roster_flight rows and pairing child tables. | medium — imports are data-volume heavy and already batch IDs/lookups, so reducing round trips should materially shorten migration/sync runs; not user-request hot path. | M | Bulk writes can make a single bad row fail more rows if savepoint boundaries change; verify with existing migration tests plus a seeded bad-record case that warnings and successful rows remain equivalent. |  |
| F-007 | quality | `gantt/src/components/scenario/crew-bids/crew-bids-view.tsx:11-175`; `gantt/src/components/scenario/crew-bids/crew-bids-view 2.tsx:11-138`; `gantt/src/components/scenario/multi-select.tsx:1-116`; `gantt/src/components/scenario/multi-select 2.tsx:1-116`; `live-server/src/routes/pbs/crew-bids.ts:1-220`; `live-server/src/routes/pbs/crew-bids 2.ts:1-220` | Duplicate `* 2.ts(x)` files make the active implementation ambiguous and invite stale fixes. | The repository contains duplicate-looking files with spaces in their names, including identical `crew-bids-api 2.ts`, `multi-select 2.tsx`, `app-dialog 2.tsx`, `live-server/src/routes/pbs/index 2.ts`, and `crew-bids 2.ts`; active imports reference the non-` 2` filenames. | Delete or archive the inactive duplicates after confirming no imports reference them, and add a repo hygiene check that fails on accidental `* 2.*` files outside generated/test-result locations. | medium — this is low-effort risk reduction for future fixes, especially around PBS and dialog UI files with active ongoing changes. | S | Removing a duplicate could break an untracked ad hoc script that imports it by path; verify with `rg` import search and TypeScript build. |  |
| F-008 | quality | `gantt/src/stores/crew-store.ts:307-331` | Debug logging remains in a production-facing Zustand store hot path. | `loadMore` emits four `console.log` statements for every pagination attempt, parameters, and result; project security guidance forbids normal browser console noise for sensitive crew data. | Remove the debug logs or route them through a development-only logger guarded by `import.meta.env.DEV`, without logging full filter payloads in production. | low — primarily reduces console noise and accidental data exposure rather than changing runtime behavior. | S | Removing logs can make manual debugging less convenient; verify load-more behavior with existing Gantt crew store/UI tests. |  |
| F-009 | quality | `live-server/src/services/crew/crew-service.ts:52-56` | `currentEffCondition` uses `as any` to satisfy Drizzle typing and weakens type guarantees in a shared service helper. | The helper casts `table.effDt as any` and `table.expDt as any`, despite project TypeScript rules banning `any` outside constrained test scaffolding. | Replace the helper signature with a typed shape compatible with Drizzle timestamp columns, or split it into typed helpers for `crewRank`, `crewBase`, `crewFleet`, `crewStatus`, and `crewTeam`. | low — this is a maintainability/type-safety improvement, not an observed runtime bug. | S | Over-tight typing can fail on one model’s column type; verify with `npm run typecheck` or the live-server verify script. |  |

Top 3 first steps summary:
1. Start with F-001: paginate or virtualize Crew Bids because it removes the largest unbounded UI/API path.
2. Pair F-001 with F-002 measurement: run `EXPLAIN (ANALYZE, BUFFERS)` before adding any PBS indexes.
3. Do F-007 next because duplicate `* 2.*` files are cheap to remove and reduce future merge/fix ambiguity.
4. Then address F-004 or F-005 depending on whether RO export or engine cleanup shows worse latency in logs.
5. Keep F-008 and F-009 as cleanup items unless they fall naturally into nearby work.

# enhance-Ver2

AI-related functions only; non-AI findings intentionally skipped.

| id | category | file + line range | problem | evidence | proposed_change | impact | effort | risk | depends_on |
|---|---|---|---|---|---|---|---|---|---|
| F-010 | performance | `ai-server/src/chat/routes.py:30-37`; `ai-server/src/llm/client.py:54-94`; `gantt/src/components/ai-chat/use-ai-chat.ts:29-44` | AI chat sends unbounded conversation history to the LLM on every request. | The frontend builds `history` from every prior thread message plus the new user message, the route forwards all messages unchanged, and `llm_tools` calls the provider with that full list. | Add a bounded chat context policy, such as keeping the last N user/assistant turns plus an optional short summary, and reject or truncate oversized messages before calling `llm_tools`. | high — prompt size grows linearly during a session, increasing latency/cost and eventually causing provider context failures; needs measurement for exact threshold. | S | Truncation can remove context needed for follow-up commands; verify with chat tests for first-turn tool calls, follow-up questions, and long-thread truncation. |  |
| F-011 | performance | `ai-server/src/chat/routes.py:30-36`; `ai-server/src/regression/routes.py:82-98`; `ai-server/src/regression/routes.py:135-171`; `ai-server/src/regression/runner.py:16-23` | AI routes run blocking LLM SDK calls and Playwright subprocess execution inside synchronous FastAPI handlers. | `chat`, `generate`, and `create_run` are normal `def` routes; they call synchronous provider clients and `subprocess.run(..., timeout=600)` before returning. | Convert AI work to explicit background jobs or async wrappers using `run_in_threadpool`/`asyncio.to_thread`, and make run creation return immediately with pollable status for both generation and Playwright execution. | high — slow LLM calls or 10-minute Playwright runs can consume server worker threads and make the AI server unresponsive under concurrent use. | M | Job orchestration can change response timing and status semantics; verify existing route tests plus concurrent chat/run smoke tests and polling behavior. |  |
| F-012 | quality | `ai-server/src/chat/routes.py:21-36`; `ai-server/src/chat/tools.py:76-97`; `gantt/src/components/ai-chat/dispatch-ai-action.ts:18-49` | AI tool inputs are weakly validated before mutating live Gantt stores. | `ChatMessage.role` accepts any string, `ChatRequest.messages` has no length/content limits, `tool_call_to_action` copies filter arrays without type/length/code normalization, and `dispatchAiAction` applies returned fields directly to Zustand stores. | Add Pydantic schemas for chat roles and each tool action, normalize airport/rank/fleet codes, cap array lengths, and drop fields that fail validation before returning actions to the frontend. | medium — this reduces malformed model output and prompt-injection side effects while preserving the same intended actions. | M | Over-strict validation can block currently accepted natural-language filters; verify tool mapping tests for valid crew/pairing/flight filters and invalid shape rejection. | F-010 |
| F-013 | performance | `ai-server/src/regression/store.py:16-25`; `ai-server/src/regression/store.py:70-100`; `ai-server/src/regression/routes.py:135-164` | RegressionStore rewrites the entire JSON store repeatedly during a single AI regression run. | `_save()` writes all `_data` with `json.dumps(..., indent=2)`, `record_run()` calls `append_version()` which calls `_save()`, then calls `_save()` again, and `create_run` calls `record_run` once per test result. | Make `record_run` mutate the run and append version with a single save, and batch store persistence for `create_run` so all test results from one run flush once. | medium — repeated full-file writes scale poorly with many generated tests and versions; exact impact depends on store size. | S | Batching can lose updates if an exception interrupts the run; verify store tests for run stats, version append, and persistence after partial result handling. |  |
| F-014 | quality | `ai-server/src/regression/routes.py:107-132`; `ai-server/src/regression/validate.py:36-49` | Applying AI-generated Playwright code relies on shallow text gates and writes executable test code into the repo. | The route writes `req.code` into `e2e/gantt/user-tests.spec.ts`; `validate_generated` checks only string markers such as `waitForTimeout`, `expect(`, and `toBeVisible` without parsing imports, filesystem/network APIs, or `test.only`. | Parse generated code with a TypeScript/ESTree validator or a stricter allowlist before writing, blocking imports, `test.only`, arbitrary Node APIs, external network calls, and non-Playwright side effects. | medium — this is a safety and maintainability guard for AI-generated executable code, not a behavior change for valid tests. | M | A stricter gate may reject useful generated tests until prompts are adjusted; verify existing generate/apply tests and add allowed/disallowed code fixtures. |  |
| F-015 | performance | `ai-server/src/llm/client.py:37-51`; `ai-server/src/llm/client.py:65-94`; `ai-server/src/llm/config.py:4-37` | LLM client construction and provider resolution are repeated for every AI request. | `llm_text` and `llm_tools` call `resolve_provider(_env_from_settings())`, then construct a fresh Anthropic or OpenAI-compatible client for each request. | Cache resolved provider configuration and SDK client instances per provider/base URL, with a small explicit refresh path for changed settings during development. | low — SDK client construction overhead is small relative to LLM latency, so this is a micro-optimization unless request volume rises. | S | Cached clients can hold stale credentials after env changes; verify tests that monkeypatch settings and add a cache-reset helper for tests. |  |
| F-016 | quality | `gantt/src/components/ai-chat/use-ai-chat.ts:45-50`; `gantt/src/components/ai-chat/ai-chat-panel.tsx:10-13`; `gantt/src/services/ai-api.ts:7-9` | AI chat error and submit handling loses useful failure state and can clear unsent input. | `submit` clears `input` immediately after `send(input)`, `useAiChat` catches all errors as the same generic assistant message, and the API client exposes no typed error details. | Clear input only after accepting a non-empty send, return structured AI errors from `aiApi.chat`, and display a concise provider/server error category without leaking secrets. | low — this improves AI UX and debuggability without changing successful behavior. | S | More detailed errors can accidentally expose provider messages; verify failed-request tests redact keys and keep input behavior correct. |  |

Top 3 AI-only first steps summary:
1. Start with F-010: bound chat history before model calls because it directly controls AI latency, cost, and failure rate.
2. Do F-011 next if more than one user can use AI tools concurrently, since blocking routes can tie up the AI server.
3. Pair F-012 with F-010 so truncated chat and validated tool actions produce predictable live-board mutations.
4. For AI-generated tests, prioritize F-014 before expanding generation usage beyond internal workflows.
5. Treat F-015 and F-016 as opportunistic cleanup unless they fall into nearby AI client work.

## enhance-Ver2 — execution status (2026-06-03)

Validated against current code, then executed (TDD; ai-server suite 43 → 53 passing; spec+quality reviewed, approved).

| id | status | where | notes |
|---|---|---|---|
| F-010 | ✅ done | `chat/routes.py` (`MAX_HISTORY_MESSAGES=12`, `MAX_MESSAGE_CHARS=4000`) | last-N turns + per-message truncation; commit `d360d20` |
| F-012 | ✅ done | `chat/routes.py` (`role: Literal`), `chat/tools.py` (cap 50 + UPPERCASE codes) | invalid role → 422; live-verified normalization; `d360d20` |
| F-013 | ✅ done | `regression/store.py` (`append_version(persist=…)`) | single `_save()` per `record_run`; `d360d20` |
| F-014 | ✅ done | `regression/validate.py` (denylist) | blocks `test.only`/`require(`/`child_process`/`eval(`/`process.`/non-playwright imports; `d360d20` |
| F-016 | ✅ done | `gantt/.../ai-chat-panel.tsx` | clear input only on accepted non-empty/non-busy send; commit `73b94ff` |
| F-011 | ⏸ deferred | — | FastAPI already threadpools `def` routes (event loop not blocked); full background-job system is an explicit v1 non-goal. Revisit if concurrent AI usage grows. |
| F-015 | ⏭ skipped | — | client construction ≪ LLM latency (negligible) and caching adds stale-cred risk. YAGNI. |

Residual (acceptable) heuristic gaps in F-014: bracket-notation `test['only']`, side-effect `import 'x'`, and dynamic `import()` are not caught — the gate complements the "emit only the test() block" prompt; a full AST/ESTree validator is the documented follow-up if generation usage broadens.
