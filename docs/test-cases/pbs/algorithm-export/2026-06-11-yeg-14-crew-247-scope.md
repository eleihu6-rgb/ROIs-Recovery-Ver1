# PBS Algorithm Export YEG 14 人员范围修正测试

## 前置条件

- `247` 已在数据库中具备本次 YEG 14 测试所需 bid 数据。
- `247` 在 PBS 用户数据中 base 为 `YEG`。
- `274` 不应再作为本次 YEG 14 临时导出的目标 crew。

## 自动化检查

运行：

```bash
cd /Users/lei/Codehub/rois-ai/pbs-server
DATABASE_URL=postgresql://test:test@localhost:5432/rois node --import tsx --test src/services/algorithm-export/algorithm-export-service.test.ts
pnpm exec tsc --noEmit
```

预期：

- `algorithm-export-service.test.ts` 通过。
- 测试断言 YEG 14 scope 包含 `247`。
- 测试断言 YEG 14 scope 不包含 `274`。
- TypeScript 检查通过。

## 手工检查

1. 调用临时导出接口：

   ```bash
   curl -H "Authorization: Bearer <admin-token>" \
     "http://localhost:3002/api/admin/algorithm-export/yeg-test-package?periodCode=Jun%202026" \
     -o /tmp/pbs-algorithm-export-yeg-14-Jun-2026.tgz
   ```

2. 解压并检查 CSV：

   ```bash
   mkdir -p /tmp/pbs-yeg-14-check
   tar -xzf /tmp/pbs-algorithm-export-yeg-14-Jun-2026.tgz -C /tmp/pbs-yeg-14-check
   rg "^(247|274)," /tmp/pbs-yeg-14-check
   ```

预期：

- 导出包中仍包含 `DAYSOFF.csv`、`PAIRING_SCORE.csv`、`RESERVE_SCORE.csv`、`LINE_RULES.csv`、`LINE_RULES_README.md`。
- 相关 CSV 中可见 `247` 的测试数据。
- 不应出现以 `274,` 开头的 YEG 14 测试导出行。
- 导出包文件名仍为 `pbs-algorithm-export-yeg-14-Jun-2026.tgz`。

## 图片中确认的新 14 人数据

| Crew_ID | 条件 |
| --- | --- |
| 247 | Line `408 Commuter Pattern`, `Between 4 / 4 / 5` |
| 383 | DaysOff `203 Min Consecutive Days Off = 7` |
| 536 | Pairing `109 Average Daily Credit > 07:00` |
| 572 | Reserve `301 PRAM whole_month`, `Award` |
| 996 | DaysOff `201 Prefer Off = Weekends` |
| 2224 | Reserve `301 PRAM first_half` + `301 PRPM second_half` |
| 2227 | Reserve `301 PRAM first_half` + Pairing `106 Departing On 2026-06-16 ~ 2026-06-30` |
| 2229 | Pairing `105 Total Credit > 20:00`, `T2 > 25:00` |
| 2377 | Pairing `106 MON/FRI/SAT/SUN` + Avoid `110 TUE/WED/THU` |
| 2440 | DaysOff `Saturday,Sunday` + Avoid Pairing `117 Redeye` |
| 2696 | DaysOff `201 Prefer Off = Weekends` |
| 2697 | DaysOff `Tuesday,Wednesday` + Reserve `301 PRAM whole_month` |
| 13697 | Line `427 Reserve award` + DaysOff `Weekends` |
| 8888 | Line `427 Reserve avoid` |
