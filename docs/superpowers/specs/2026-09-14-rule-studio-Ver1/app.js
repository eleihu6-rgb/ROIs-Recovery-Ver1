/* Design interaction only. No API, LLM, engine, database or deployment connection. */
const concept=document.body.dataset.concept;
document.body.classList.add(concept);
const names=['Understand & match','Clarify','Define the rule','Specify tests','Build candidate','Validate','Review package'];
const subtitles=['Link policy clauses to existing capabilities.','Resolve the questions that change the result.','Confirm the meaning before implementation.','Approve expected outcomes and coverage.','Review a change limited to this rule.','Prove individual behavior, then optimizer behavior.','Review one version with all its evidence.'];
const state={step:concept==='dossier'?3:2,rev:1,x:7,y:2,mode:'separate',blank:true,group:true,history:true,approved:new Set([0,1]),built:false,individual:false,optimizer:false,run:false,stale:false,showHistory:false,selected:0,notice:'',chat:{},days:['FLY','DO','FLY','FLY','BLANK','FLY','FLY'],versions:[{rev:1,note:'Initial sample contract · separate days',x:7,y:2,mode:'separate',blank:true,group:true,history:true}],packet:false};
const titles={workbench:'A · Guided Workbench',dossier:'B · Evidence Dossier',lab:'C · Visual Rule Lab'};
const escape=s=>String(s).replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
const badge=(s,t='')=>`<span class="badge ${t}">${s}</span>`;
const action=(label,act,cls='',disabled=false)=>`<button class="${cls}" data-action="${act}" ${disabled?'disabled':''}>${label}</button>`;
const modeLabel=()=>state.mode==='separate'?'separate days':'consecutive days';
function stepStatus(i){if(i<4)return state.approved.has(i)?'Approved · demo':(state.stale&&i>=2?'Needs review':'Draft');if(i===4)return state.built?'Built · demo':state.stale?'Out of date':'Not built';if(i===5)return state.optimizer?'Passed · demo':state.run&&!state.individual?'Failed · demo':state.individual?'Individual passed':'Not run';return state.packet?'Packet prepared · demo':state.optimizer?'Ready for review':'Blocked';}
function contract(){return `In <b>every rolling ${state.x}-day window</b>, a crew member must have at least <b>${state.y} ${modeLabel()}</b>. Count ${state.blank?'complete blank days':''}${state.blank&&state.group?' or ':''}${state.group?'full days in the Days Off assignment group':''}${!state.blank&&!state.group?'<b>no eligible dates — review definition</b>':''}.`;}
function header(){return `<header class="topbar"><div class="brand"><span class="brandmark">R</span>ROIS Rule Studio <span class="badge">Design exploration</span></div><nav class="toplinks" aria-label="Design alternatives"><a href="index.html">Overview</a>${['workbench','dossier','lab'].map((c,i)=>`<a class="${c===concept?'current':''}" href="${c}.html">${['A · Workbench','B · Dossier','C · Rule Lab'][i]}</a>`).join('')}<a href="requirements.md">Requirements ↗</a></nav></header><div class="demo">INTERACTIVE DESIGN ONLY · Sample policy, scripted assistant, simulated evidence · No live rules or data are changed</div><section class="intro"><div><div class="eyebrow">${titles[concept]} / Days off · DO-DEMO-01</div><h1>${concept==='dossier'?'A rule with a complete evidence trail':concept==='lab'?'See the rule. Challenge the result.':'From policy to a proven roster rule'}</h1><p>${concept==='dossier'?'Follow each clause through interpretation, tests and review.':concept==='lab'?'Change an example, inspect the logic, then prove it at scale.':'A guided process for airline specialists, rule engineers and QA.'}</p></div><div class="actions">${badge('Draft v'+state.rev,'blue')}${action('Version history','history')}${action('Reset demo','reset','small')}</div></section>`;}
function rail(){const items=names.map((n,i)=>`<button class="step ${state.step===i?'active':''}" data-step="${i}" aria-current="${state.step===i?'step':'false'}"><span class="num">${i+1}</span><span><strong>${n}</strong><small>${stepStatus(i)}</small></span></button>`).join('');if(concept==='dossier')return `<aside class="card ledger"><div class="eyebrow">Source dossier</div><h2>Days off entitlement</h2><div class="docmeta">${badge('Sample policy')}<span class="mono">§ 4.2 / v1</span></div><div class="quote">“In any seven-day period, crew shall receive two days off.”<br><small>Invented clause for design discussion. Definition still requires review.</small></div><h3>Evidence ledger</h3>${items}<div class="railnote">Trace ID: DO-DEMO-01<br>Clause → contract → case → run → approval</div></aside>`;return `<aside class="card"><div class="panelhead">Rule lifecycle ${badge('7 steps')}</div><nav class="steps" aria-label="Rule lifecycle">${items}</nav><div class="railnote"><b>Return to any step.</b><br>Editing creates a revision. Dependent evidence must be renewed.</div><div class="railnote">Execution scope<br>${badge('Rule package only','blue')}<p>Other modules require a separate engineering change.</p></div></aside>`;}
function nodeflow(){return `<nav class="nodeflow" aria-label="Rule lifecycle">${names.map((n,i)=>`<button class="node ${i===state.step?'active':''}" data-step="${i}"><small class="mono">0${i+1} ${i<6?'→':''}</small><b>${n}</b><small>${stepStatus(i)}</small></button>`).join('')}</nav>`;}
function history(){return state.showHistory?`<section class="history"><div class="row"><h3>Immutable revisions · demonstration</h3>${badge('Current v'+state.rev,'blue')}</div><p class="muted">Restoring creates a new draft. Previous evidence remains attached to its original version.</p><div class="historygrid">${state.versions.map(v=>`<div class="historyitem"><b>v${v.rev} · ${v.x} days / ${v.y} off / ${v.mode}</b><p>${escape(v.note)}</p>${v.rev!==state.rev?`<button class="small" data-restore="${v.rev}">Create draft from v${v.rev}</button>`:badge('Current draft')}</div>`).join('')}</div></section>`:'';}
function calendar(editable=false,caseMode=null,caseDays=null,fixture=false){const days=caseDays||state.days,mode=caseMode||state.mode,x=fixture?7:state.x,y=fixture?2:state.y;const counts=days.map(d=>(d==='BLANK'&&(fixture||state.blank))||(d==='DO'&&(fixture||state.group)));const total=counts.filter(Boolean).length;let run=0,longest=0;counts.forEach(o=>{run=o?run+1:0;longest=Math.max(longest,run)});const sufficient=x===7;const legal=(mode==='separate'?total:longest)>=y;return `<div class="casebox"><div class="row"><h3>${editable?'Example roster · click a day to cycle assignment':'Illustrative crew roster · DEMO-CA-01'}</h3>${badge('7-day slice')}</div><small>14–20 Sep 2026 · Home-base calendar dates · Simplified design illustration</small><div class="calendar">${days.map((d,i)=>`<${editable?'button':'div'} ${editable?`data-day="${i}" aria-label="Change day ${i+1}, currently ${d}"`:''} class="day ${counts[i]?'off':''}"><small>${['Mon','Tue','Wed','Thu','Fri','Sat','Sun'][i]} ${14+i}</small><span>${d==='BLANK'?'— blank':d}</span><small>${counts[i]?'counts':d==='FLY'?'work':'excluded'}</small></${editable?'button':'div'}>`).join('')}</div><div class="casefoot"><span>Eligible <b>${total}</b> · Longest run <b>${longest}</b> · Required <b>${y}</b> · ${mode}</span>${badge(!sufficient?'Incomplete preview':legal?'Example legal':'Example violation',!sufficient?'warn':legal?'good':'bad')}</div><small>${sufficient?'This evaluates the visible example only. It is not a Rust or Gantt test.':'Preview contains only 7 days; the complete '+x+'-day rule cannot be evaluated here.'}</small></div>`;}
function labcanvas(){return `<section class="labcanvas"><div class="row"><h3>Rule map</h3>${badge('Select a node to edit','blue')}</div><div class="visualrule"><button class="logicnode" data-step="1">01 / Scope<b>Every crew window</b><small>Home-base calendar</small></button><button class="logicnode" data-step="2">02 / Window<b>${state.x} rolling days</b><small>Inclusive start · exclusive end</small></button><button class="logicnode" data-step="2">03 / Qualify<b>${state.blank?'Blank + ':''}${state.group?'Days Off group':'No group'}</b><small>Duty overlap excludes day</small></button><button class="logicnode" data-step="3">04 / Assert<b>≥ ${state.y} ${state.mode==='separate'?'off days':'in a row'}</b><small>Explain failing window</small></button></div>${calendar(true)}</section>`;}
function fields(){return `<form id="contract-form"><div class="fields"><label class="field">Window length X<input name="x" type="number" min="1" max="365" step="1" value="${state.x}" required><small>Mock supports 1–365; production range TBD.</small></label><label class="field">Minimum days off Y<input name="y" type="number" min="1" max="365" step="1" value="${state.y}" required></label><label class="field">How days are counted<select name="mode"><option value="separate" ${state.mode==='separate'?'selected':''}>Separate days · total ≥ Y</option><option value="consecutive" ${state.mode==='consecutive'?'selected':''}>Consecutive · one run ≥ Y</option></select></label><label class="field">Window / timezone<input value="Rolling / home-base calendar" readonly aria-label="Proposed window and timezone"></label></div><label class="check"><input name="blank" type="checkbox" ${state.blank?'checked':''}>Count a completely blank day when roster data is complete</label><label class="check"><input name="group" type="checkbox" ${state.group?'checked':''}>Count full-day assignments in Days Off group · demo snapshot: DO, OFF</label><label class="check"><input name="history" type="checkbox" ${state.history?'checked':''}>Require complete lead-in and look-ahead before evaluating</label><div class="notice warn" style="margin-top:var(--s4)">Saving creates a new revision. Contract/test approvals and downstream build/run evidence become out of date. Source and clarification remain attached.</div><div class="actions" style="margin-top:var(--s3)"><button type="submit" class="primary">Save as new revision</button><span class="muted">No live parameter changes</span></div></form>`;}
const cases=[['DO-01','Two separate off dates','Legal in separate mode'],['DO-02','Same dates, consecutive mode','Violation · longest run 1'],['DO-03','Two adjacent off dates','Legal in both modes'],['DO-05','Unknown roster history','Indeterminate · block evaluation'],['DO-07','Off code overlaps duty','Day excluded from off count'],['DO-09','Window crosses month end','Load complete edge context']];
function testTable(results=false){return `<div class="tablewrap"><table><thead><tr><th>Case / linked clause §4.2</th><th>Expected result</th><th>${results?'Assertion result':'Coverage state'}</th></tr></thead><tbody>${cases.map((c,i)=>`<tr class="testrow ${state.selected===i?'selected':''}"><td><button class="small" data-case="${i}">${c[0]} · ${c[1]}</button></td><td>${c[2]}</td><td>${results&&state.run?badge(i===3&&!state.history?'FAIL':'PASS',i===3&&!state.history?'bad':'good'):badge(state.stale?'Stale / rerun':'Not run')}</td></tr>`).join('')}</tbody></table></div>`;}
function caseDetail(){const c=cases[state.selected];return `<div class="casebox"><div class="row"><h3>${c[0]} · ${c[1]}</h3>${badge('Selected case','blue')}</div><dl class="definition"><dt>Fixture</dt><dd>Pinned example: X=7, Y=2, complete blank + Days Off enabled. Parameter expansion belongs to the full suite.</dd><dt>Expected</dt><dd>${c[2]}</dd><dt>Actual</dt><dd>${!state.run?'Not run':state.selected===3&&!state.history?'Incorrectly legal — incomplete history was treated as blank':c[2]+' (scripted outcome)'}</dd><dt>Evidence</dt><dd>${state.run?'Demo run R'+state.rev+' · v'+state.rev+' · simulated fixture / no real engine logs':'Real fixture, screenshot, trace and engine hash will attach after execution.'}</dd><dt>Traceability</dt><dd>Sample §4.2 → contract v${state.rev} → ${c[0]}</dd></dl>${state.selected===3&&state.run&&!state.history?`<div class="notice warn">Failure: missing data must not appear as a legal blank day. Return to the contract and enable the completeness requirement.</div>${action('Discuss and revise definition','fix','primary')}`:''}${state.selected<3?calendar(false,state.selected===1?'consecutive':'separate',state.selected===2?['FLY','DO','DO','FLY','FLY','FLY','FLY']:['FLY','DO','FLY','FLY','BLANK','FLY','FLY'],true):''}</div>`;}
function content(){switch(state.step){case 0:return `<div class="notice">Start with customer policy, then compare meaning with the existing library. All documents below are examples.</div><div class="match"><div class="row"><h3>Sample union policy · §4.2</h3>${badge('Source reviewed · demo','good')}</div><p>In any seven-day period, crew shall receive two days off. “Any” and “day off” need interpretation.</p></div><div><h3>Existing-rule candidates · source-inspected</h3><div class="match"><div class="row"><b>7505 · Minimum days off</b>${badge('Partial match','warn')}</div><p>Rust available. Counts off days within a roster period; supports blank/code/group behavior. Rolling windows and consecutive semantics still need a gap review.</p></div><div class="match"><div class="row"><b>7508 · Rolling free days</b>${badge('Related rule','blue')}</div><p>Rust available. Rolling calendar free-day logic; its eligibility/rest definition differs from this proposed policy.</p></div><div class="match"><div class="row"><b>Minimum consecutive off days</b>${badge('Candidate gap','warn')}</div><p>No direct implementation identified in the bounded source inspection. Legacy inventory review remains necessary.</p></div></div><div class="notice warn">Recommended disposition: extend approved primitives after semantic review. Similarity is not proof of compliance.</div>`;
case 1:return `<div class="contract">“${escape('In any seven-day period, crew shall receive two days off.')}”</div><div class="tablewrap"><table><thead><tr><th>Question</th><th>Proposed interpretation</th><th>Status</th></tr></thead><tbody><tr><td>Any period?</td><td>Every rolling X-day window</td><td>${badge('Demo assumption','warn')}</td></tr><tr><td>Separate or together?</td><td>${modeLabel()}</td><td>${badge('Editable in step 3','blue')}</td></tr><tr><td>What is a blank day?</td><td>Complete day with no assignments; unknown data never qualifies</td><td>${badge('Confirm with airline','warn')}</td></tr><tr><td>Which timezone?</td><td>Crew home base; DST-aware dates</td><td>${badge('Confirm with airline','warn')}</td></tr><tr><td>Off code plus duty?</td><td>Overlapping duty disqualifies the date</td><td>${badge('Confirm with airline','warn')}</td></tr></tbody></table></div><div class="notice">The live interview asks one focused question at a time and records the decision plus a counterexample. Use the side chat to leave a stage-specific note in this demo.</div>${action('Edit proposed answers in contract','define','primary')}`;
case 2:return `<div class="contract">${contract()}</div>${fields()}<dl class="definition"><dt>Precedence</dt><dd>Overlapping work disqualifies a day, even if an off code is present.</dd><dt>Window edge</dt><dd>Incomplete history is indeterminate; never silently count it as blank.</dd><dt>Approval</dt><dd>Applies to this exact contract and group snapshot.</dd></dl>${concept==='lab'?'':calendar()}`;
case 3:return `<div class="notice">Coverage is a reviewed matrix, not a claim to test every possible roster. These six rows preview a larger planned suite.</div><div class="metrics"><div class="metric"><b>16</b><span>Requirement / risk families in brief</span></div><div class="metric"><b>4</b><span>Layers: unit, Gantt, optimizer, perf</span></div><div class="metric"><b>0</b><span>Real runs in this design mock</span></div></div>${testTable()}${caseDetail()}<div class="notice warn">Optimizer plan: pinned datasets + seeds + engine build; compare rule latency, wall time, memory, coverage and final legality. Example tolerance +5% only; airline acceptance pending.</div><div class="actions">${action('Load missing-context failure example','fault')}<small>Creates a draft with a deliberate semantic gap.</small></div>`;
case 4:return `<div class="notice ${state.built?'good':''}">${state.built?'Simulated build completed. Candidate evidence is attached to v'+state.rev+'.':'Build is gated by source, clarification, contract and test-plan approval.'}</div><div class="flowline">Approved contract → scoped rule candidate → engineer review → test artifact</div><div class="reviewlist"><div><span>Rule behavior</span><b>${state.x} days / ${state.y} ${modeLabel()}</b></div><div><span>Permitted change</span><span>Selected rule + registry + rule tests</span></div><div><span>Other system files</span>${badge('Denied','good')}</div><div><span>Customer data / credentials</span>${badge('No access','good')}</div><div><span>Candidate status</span>${badge(state.built?'Built · simulated':'Not built',state.built?'good':'')}</div></div><div class="notice warn">A shared data-model or connector change becomes “Platform extension required.” The rule agent cannot expand its own edit scope.</div>${action(state.built?'Rebuild candidate · demo':'Simulate candidate build','build','primary',![0,1,2,3].every(i=>state.approved.has(i)))}<small>Review panel represents a future Rust diff. This mock generates no source code.</small>`;
case 5:return `<div class="metrics"><div class="metric"><b>${state.run?(state.individual?'6/6':'5/6'):'0/6'}</b><span>Scripted individual assertions</span></div><div class="metric"><b>${state.optimizer?'PASS':'—'}</b><span>Optimizer + perf sample</span></div><div class="metric"><b>v${state.rev}</b><span>Evidence must match draft</span></div></div><div class="actions">${action('Simulate individual suite','individual','primary',!state.built)}${action('Simulate optimizer benchmark','optimizer','',!state.individual)}</div>${!state.built?'<div class="notice warn">Build the current revision before running the individual suite.</div>':''}${state.run&&!state.individual?'<div class="notice warn">DO-05 failed: missing history was counted as blank. Optimizer is blocked. Select the case to review and revise.</div>':''}${testTable(true)}${caseDetail()}<div class="match"><div class="row"><h3>Mass-assignment gate</h3>${badge(state.optimizer?'Passed · simulated':state.individual?'Ready to run':'Blocked',state.optimizer?'good':state.individual?'blue':'warn')}</div><p>${state.optimizer?'Illustrative cohort: 500 crew / 8,000 pairings. Baseline wall time 100s → candidate 103s (+3%). Rule p95 10µs → 10.3µs (+3%). Both under the demo +5% tolerance. Final legality and coverage checks: sample PASS. No solver was executed.':'Run only after all required individual layers pass. Pin data, seeds, ruleset and builds; report correctness and performance separately.'}</p></div>`;
case 6:return `<div class="notice ${state.optimizer?'good':'warn'}">${state.optimizer?'All demonstrated gates are current. Ready for independent review.':'Review package blocked: current build, individual and optimizer evidence are required.'}</div><div class="contract">${contract()}</div><div class="reviewlist">${[['Source + clarification',state.approved.has(0)&&state.approved.has(1)],['Contract + test plan',state.approved.has(2)&&state.approved.has(3)],['Candidate build',state.built],['Individual assertions',state.individual],['Optimizer + performance',state.optimizer]].map(([n,v])=>`<div><span>${n}</span>${badge(v?'Current · demo':'Missing / stale',v?'good':'warn')}</div>`).join('')}<div><span>Independent release approval</span>${badge('Not granted','warn')}</div></div><div class="notice">The packet records source, contract, parameter/group versions, tests, artifact hash and run evidence. Release owner selects customer, effective date and a compatible rollback version.</div>${action('Prepare review packet · demo','packet','primary',!state.optimizer)}${state.packet?'<div class="receipt"><b>Demo packet prepared.</b> v'+state.rev+' · For review only. No activation or deployment occurred.</div>':''}<small>Activation is a separate authorized product action, outside this prototype.</small>`;}}
const prompts=["I found related Rust rules, but their window and off-day definitions differ. Which source clause and exceptions establish an exact match?","Does “two days off” mean any two dates or one two-day block? I would ask for a roster that passes one interpretation and fails the other.","A blank date and an unknown date must be different states. Also confirm whether off days must cover a full local calendar date.","Include a test that expects a violation. A passing test can prove a roster is illegal. Who independently approves the expected result?","I can propose a rule-scoped change. A new connector field needs a separate platform task and cannot be added by this rule session.","If a case fails, first classify the cause: requirement, implementation, fixture, integration or performance. Do not change expected results just to pass.","An approval must bind to this exact version. If the contract changes, preserve old evidence and block release until affected tests rerun."];
function chat(){return `<aside class="card chat"><div class="panelhead"><span class="row"><span class="avatar">AI</span>Rule assistant</span>${badge('Step '+(state.step+1))}</div><div class="chatbody"><div class="eyebrow">Scripted design conversation · v${state.rev}</div><div class="bubble">${prompts[state.step]}</div>${(state.chat[state.step]||[]).map(m=>`<div class="bubble ${m.role==='user'?'user':''}"><b>${m.role==='user'?'You':'Demo assistant'} · v${m.rev}</b><p>${escape(m.text)}</p></div>`).join('')}<button class="suggestion" data-action="suggest">${state.step===2?'Ask about separate vs consecutive':state.step===5?'Explain how to investigate a failure':'Show a clarification example'}</button></div><form id="chat-form" class="chatfooter"><label class="field">Discuss this step<textarea name="message" placeholder="Add a question or missing requirement…" required></textarea></label><div class="actions"><small>Saved in this tab only</small><button class="primary" type="submit">Add note</button></div></form><div class="timeline"><h3>Decision trail</h3><p style="margin-top:var(--s2)">Source §4.2 → contract v${state.rev}</p><p>${state.stale?'Earlier evidence preserved; dependent steps need new approval.':'Only approved decisions become rule behavior.'}</p><small>Chat never silently changes the contract.</small></div></aside>`;}
function footer(){return `<div class="footer"><div class="actions">${action('← Previous','prev','',state.step===0)}<small>Step ${state.step+1} of 7</small></div><div class="actions">${state.step<4?action(state.approved.has(state.step)?'Approved · demo':'Approve this step · demo','approve','',state.approved.has(state.step)||(state.step>0&&!state.approved.has(state.step-1))):''}${action(state.step===6?'Back to definition':'Next step →',state.step===6?'define':'next','primary')}</div></div>`;}
function render(){document.getElementById('app').innerHTML=header()+history()+(state.notice?`<div class="banner notice" role="status">${escape(state.notice)}</div>`:'')+(concept==='lab'?nodeflow():'')+`<div class="layout">${concept==='lab'?'':rail()}<main class="card">${concept==='lab'?labcanvas():''}${concept==='dossier'?`<div class="tracebar">POLICY §4.2 → DO-DEMO-01 → CONTRACT v${state.rev} → ${state.step>=3?'CASE REGISTER':'INTERPRETATION'}</div>`:''}<div class="stagehead"><div><div class="eyebrow">Step ${state.step+1} / ${concept==='dossier'?'Review sheet':'Rule lifecycle'}</div><h2>${names[state.step]}</h2><p>${subtitles[state.step]}</p></div>${badge(stepStatus(state.step),state.approved.has(state.step)?'good':'blue')}</div><div class="stagebody">${content()}</div>${footer()}</main>${chat()}</div>`;}
function revise(values,note){Object.assign(state,values);state.rev++;state.approved=new Set([0,1]);state.built=false;state.individual=false;state.optimizer=false;state.run=false;state.packet=false;state.stale=true;state.versions.push({rev:state.rev,note,x:state.x,y:state.y,mode:state.mode,blank:state.blank,group:state.group,history:state.history});state.notice='Draft v'+state.rev+' created. Steps 3–7 need fresh approval or evidence; previous revisions are preserved.';}
document.addEventListener('click',e=>{const b=e.target.closest('button');if(!b||b.disabled)return;if(b.dataset.step!==undefined){state.step=Number(b.dataset.step);state.notice='';render();return;}if(b.dataset.day!==undefined){const i=Number(b.dataset.day);const opts=['FLY','DO','BLANK'];state.days[i]=opts[(opts.indexOf(state.days[i])+1)%3];state.notice='Example roster changed only. Approve and version a test specification separately to use it as evidence.';render();return;}if(b.dataset.case!==undefined){state.selected=Number(b.dataset.case);render();return;}if(b.dataset.restore){const v=state.versions.find(v=>v.rev===Number(b.dataset.restore));revise({x:v.x,y:v.y,mode:v.mode,blank:v.blank,group:v.group,history:v.history},'Restored from v'+v.rev);render();return;}const a=b.dataset.action;if(!a)return;state.notice='';switch(a){case 'history':state.showHistory=!state.showHistory;break;case 'reset':location.reload();return;case 'prev':state.step=Math.max(0,state.step-1);break;case 'next':state.step=Math.min(6,state.step+1);break;case 'define':state.step=2;break;case 'approve':state.approved.add(state.step);state.notice='Demo approval recorded for step '+(state.step+1)+' at v'+state.rev+'.';break;case 'build':state.built=true;state.stale=false;state.run=false;state.individual=false;state.optimizer=false;state.packet=false;state.notice='Simulated rule candidate built for v'+state.rev+'. No Rust code generated.';break;case 'individual':state.run=true;state.individual=state.history;state.optimizer=false;state.packet=false;state.selected=state.history?0:3;state.notice='Scripted individual suite: '+(state.history?'6/6 PASS.':'5 PASS, 1 FAIL. Optimizer blocked.');break;case 'optimizer':state.optimizer=true;state.notice='Scripted optimizer and performance evidence attached. These numbers are illustrative, not measurements.';break;case 'fault':revise({history:false},'Deliberate missing-history gap for failure walkthrough');state.step=2;break;case 'fix':state.step=2;state.notice='Enable the complete-history requirement, save a revision, approve contract/tests, rebuild and rerun.';break;case 'packet':state.packet=true;state.notice='Demo review packet prepared; release approval remains pending.';break;case 'suggest':state.chat[state.step]??=[];state.chat[state.step].push({role:'assistant',rev:state.rev,text:state.step===2?'Example: off on Tuesday and Friday satisfies two separate days, but fails a requirement for two consecutive days. Use the count-mode field to create a revised contract.':'Record a concrete passing roster and a failing counterexample, then attach the decision to this step. This response is scripted.'});break;}render();});
document.addEventListener('submit',e=>{e.preventDefault();const f=e.target;if(f.id==='contract-form'){const d=new FormData(f),x=Number(d.get('x')),y=Number(d.get('y'));if(!Number.isInteger(x)||!Number.isInteger(y)||x<1||x>365||y<1||y>x){state.notice='Invalid parameters: use whole days with 1 ≤ Y ≤ X ≤ 365 for this mock.';render();return;}if(!d.has('blank')&&!d.has('group')){state.notice='Select at least one source of qualifying off days.';render();return;}revise({x,y,mode:d.get('mode'),blank:d.has('blank'),group:d.has('group'),history:d.has('history')},'Contract changed; downstream evidence invalidated');render();}if(f.id==='chat-form'){const d=new FormData(f),msg=String(d.get('message')||'').trim();if(!msg)return;state.chat[state.step]??=[];state.chat[state.step].push({role:'user',rev:state.rev,text:msg},{role:'assistant',rev:state.rev,text:'Note recorded for this step. In the product, I would ask a focused follow-up and propose a tracked decision. This mock does not interpret your text or update the contract.'});render();}});
render();
