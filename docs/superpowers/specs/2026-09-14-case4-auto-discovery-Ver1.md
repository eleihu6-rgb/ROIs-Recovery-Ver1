# Case 4 automatic solution discovery
User approved removing the manual Find roster options step. On saved pairing detail load,
including after Build pairing, default to the first open rank and automatically generate,
price and legality-check all three existing staffing strategies. Rank/ruleset changes refresh.
Retain shared strategy/cost-tier tables, previews and draft/save behavior. Cases 1–3 unchanged.

Discovery uses the loaded Live snapshot; roster ordering/crew floating must not erase Preview.
Generation guards discard stale results. Actual roster/pairing changes retain fingerprint guards.
Verify real saved pairing 152689 read-only, automatic CA/FO discovery, all strategies,
Preview return, Help and legacy regressions. No data writes or fixture resets.
