# Rule 8030 Preview Grouping Plan

1. Add a pure confirmation-group builder that groups pairing-level 8030
   findings and extracts crew age details.
2. Render grouped cards and group-based severity counts in the shared
   `RuleConfirmDialog`.
3. Migrate the touched dialog to the standard `AppDialog`.
4. Add unit and real-gesture Playwright regression coverage.
5. Run TypeScript, UI-standard, and focused regression checks.
