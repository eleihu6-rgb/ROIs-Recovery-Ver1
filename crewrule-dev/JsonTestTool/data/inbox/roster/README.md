# JsonTestTool inbox (roster)

Drop RuleSrv-style inbox files here (e.g. `*_loadScenario.data.txt`, `*_checkPairing.txt`, `*_checkCrew.txt`).
`JsonTestTool.exe` reads this folder (by default) in filename order and runs the same scenario load/update/check flow without starting `RuleSrv`.

