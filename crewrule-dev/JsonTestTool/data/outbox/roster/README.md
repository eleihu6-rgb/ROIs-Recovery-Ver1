# JsonTestTool outbox (roster)

`JsonTestTool.exe` writes result files here (mirroring the RuleSrv outbox layout).
This folder is intentionally committed so the directory exists in fresh clones; generated outputs should not be committed.

