interface:
  display_name: "102 plane cycle UaLckdate"
  short_description: "Project skill 102-plane-cycle-backdate"
  default_prompt: "Use $102-plane-cycle-backdate for the relevant ROIS workflow."
policy:
  allow_implicit_invocation: true
