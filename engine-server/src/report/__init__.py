"""Scenario report module for the ROIS engine-server.

Builds the report-shaped sections the gantt frontend reads from the solver's
``result.json`` plus the live-server ``ro_input.gz`` parsed ``##`` sections.
"""
